name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      # ponytail: mac is arm64-only (runner arch) — ffmpeg-static installs per-arch, so a
      # cross-arch/universal build would ship the wrong ffmpeg. Add an x64 job if Intel users ask.
      - run: npm run dist
      # ponytail: publish via gh, not electron-builder's publisher — no provider config needed
      - run: |
          shopt -s nullglob
          files=(dist/*.AppImage dist/*.deb dist/*.exe dist/*.dmg dist/*.zip)
          gh release create "$GITHUB_REF_NAME" --title "$GITHUB_REF_NAME" --generate-notes || true
          gh release upload "$GITHUB_REF_NAME" "${files[@]}" --clobber
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
